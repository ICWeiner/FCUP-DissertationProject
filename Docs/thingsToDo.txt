
additional modules

send "ctrl + c" before sending commands and general sanitization of inputs in VMs

switch to netmiko's "send_command_timing" if bug rearises

Regular Expression traceroute

Insert authentication(jwt) in endpoints that should be protected, such as creation and starting, stoping of VMs

make vm creation logic more scalable (asynchronous calls to proxmox's API) (test with gunicorn asynchronous workers) (celery might be good option)

switch from sqlite3 to a "real" DB system like PostgreSQL

templates de templates, varios exs com uma topologia

test time to vm creation with a larger amount

Separate back-end from front-end (CSR and backend json replies)

Make database more complete in terms of evaluation

separate exercise creation from template vm creation