# config.yaml
---
inventory:
     plugin: SimpleInventory
     options:
          host_file: 'application/tests/inventory/hosts.yaml'
          group_file: 'application/tests/inventory/groups.yaml'
          defaults_file: 'application/tests/inventory/defaults.yaml'

runner:
     plugin: threaded
     options:
          num_workers: 2

